openapi: 3.1.0

info:
  title: UUON Clouud API
  version: 3.333

paths:

  /api/conversations:
    post:
      summary: Create reasoning session

  /api/conversations/{id}/messages:
    post:
      summary: Send message to Clouud engine

  /api/provenance/verify:
    post:
      summary: Verify generated output

  /api/ellomental/verify:
    post:
      summary: Verify response signature

  /api/health:
    get:
      summary: Engine status
