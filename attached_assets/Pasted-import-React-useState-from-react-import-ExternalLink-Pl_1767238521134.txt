import React, { useState } from 'react';
import { ExternalLink, Play, BookOpen, Code, Microscope, X } from 'lucide-react';

// Example shape data structure that would come from your API
const SHAPE_DATA = {
  'sphere': {
    name: 'Perfect Sphere',
    category: 'Basic Shapes',
    description: 'A mathematically perfect sphere with uniform curvature. The most fundamental 3D geometric primitive, representing symmetry and optimization in nature.',
    whatIsIt: 'A sphere is the set of all points equidistant from a center point. In differential geometry, it has constant positive Gaussian curvature and is the solution to many optimization problems.',
    whyItMatters: [
      'Minimal surface area for a given volume (soap bubbles, cells)',
      'Appears throughout nature (planets, atoms, droplets)',
      'Fundamental to physics (spherical coordinates, field equations)',
      'Used in engineering for bearings, tanks, and pressure vessels'
    ],
    applications: [
      'Molecular modeling (electron orbitals)',
      'Aerospace (fuel tanks, pressure vessels)',
      'Architecture (geodesic domes)',
      'Physics simulations (particle collisions)'
    ],
    technicalDetails: {
      equation: 'x² + y² + z² = r²',
      parameters: ['radius r', 'center (x₀, y₀, z₀)'],
      properties: ['Genus 0', 'Gaussian curvature K = 1/r²', 'Surface area = 4πr²', 'Volume = (4/3)πr³']
    },
    relatedShapes: ['Ellipsoid', 'Torus', 'Geodesic Sphere'],
    researchPapers: [
      'Minimal Surfaces and Sphere Packing',
      'Spherical Harmonics in Quantum Mechanics'
    ]
  },
  'kuen_surface': {
    name: 'Kuen Surface',
    category: 'Minimal Surfaces',
    description: 'A classical minimal surface with constant negative Gaussian curvature. Named after Theodor Kuen, it represents one of the earliest examples of a complete minimal surface.',
    whatIsIt: 'The Kuen surface is a minimal surface (mean curvature = 0) that extends infinitely in one direction while remaining bounded in another. It has constant negative Gaussian curvature of -1.',
    whyItMatters: [
      'Example of hyperbolic geometry in 3D space',
      'Models stress distribution in elastic materials',
      'Appears in soap film experiments',
      'Theoretical foundation for understanding curved space'
    ],
    applications: [
      'Material science (stress analysis)',
      'Differential geometry research',
      'Architectural surface design',
      'Mathematical visualization'
    ],
    technicalDetails: {
      equation: 'Parametric: x(u,v) = (cos u sinh v) / (1 + cos u cosh v)',
      parameters: ['u ∈ [0, 2π]', 'v ∈ ℝ'],
      properties: ['Mean curvature H = 0', 'Gaussian curvature K = -1', 'Asymptotic to catenoid']
    },
    relatedShapes: ['Catenoid', 'Helicoid', 'Enneper Surface'],
    researchPapers: [
      'Minimal Surfaces in Differential Geometry',
      'Complete Minimal Surfaces with Finite Total Curvature'
    ]
  }
};

const ShapeLandingPortal = () => {
  const [selectedShape, setSelectedShape] = useState('sphere');
  const [showTool, setShowTool] = useState(false);
  const shape = SHAPE_DATA[selectedShape];

  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-900 via-purple-900 to-slate-900 text-white">
      {/* Header */}
      <header className="border-b border-purple-500/30 bg-black/30 backdrop-blur">
        <div className="container mx-auto px-6 py-4 flex items-center justify-between">
          <div className="flex items-center gap-3">
            <div className="w-10 h-10 bg-gradient-to-br from-purple-500 to-pink-500 rounded-lg flex items-center justify-center font-bold">
              Ω
            </div>
            <div>
              <h1 className="text-xl font-bold">Ωmension Mathematical Universe</h1>
              <p className="text-xs text-purple-300">UUON Foundation</p>
            </div>
          </div>
          <nav className="flex gap-6 text-sm">
            <a href="/" className="hover:text-purple-300 transition">Home</a>
            <a href="/shapes" className="hover:text-purple-300 transition">Shapes</a>
            <a href="/api" className="hover:text-purple-300 transition">API</a>
            <a href="/research" className="hover:text-purple-300 transition">Research</a>
          </nav>
        </div>
      </header>

      {/* Main Content */}
      <main className="container mx-auto px-6 py-12">
        {/* Shape Selector (simulate routing) */}
        <div className="mb-8 flex gap-4">
          <button
            onClick={() => setSelectedShape('sphere')}
            className={`px-4 py-2 rounded-lg transition ${
              selectedShape === 'sphere' 
                ? 'bg-purple-600' 
                : 'bg-slate-800 hover:bg-slate-700'
            }`}
          >
            Sphere
          </button>
          <button
            onClick={() => setSelectedShape('kuen_surface')}
            className={`px-4 py-2 rounded-lg transition ${
              selectedShape === 'kuen_surface' 
                ? 'bg-purple-600' 
                : 'bg-slate-800 hover:bg-slate-700'
            }`}
          >
            Kuen Surface
          </button>
        </div>

        {/* Hero Section */}
        <div className="grid lg:grid-cols-2 gap-12 mb-16">
          <div>
            <div className="text-sm text-purple-300 mb-2">{shape.category}</div>
            <h1 className="text-5xl font-bold mb-6 bg-gradient-to-r from-purple-400 to-pink-400 bg-clip-text text-transparent">
              {shape.name}
            </h1>
            <p className="text-xl text-slate-300 mb-8 leading-relaxed">
              {shape.description}
            </p>
            
            {/* CTA Buttons */}
            <div className="flex gap-4">
              <button 
                onClick={() => setShowTool(true)}
                className="flex items-center gap-2 px-6 py-3 bg-gradient-to-r from-purple-600 to-pink-600 rounded-lg font-semibold hover:from-purple-500 hover:to-pink-500 transition transform hover:scale-105"
              >
                <Play size={20} />
                Generate 3D Model
              </button>
              <button className="flex items-center gap-2 px-6 py-3 bg-slate-800 rounded-lg font-semibold hover:bg-slate-700 transition">
                <Code size={20} />
                View API Docs
              </button>
            </div>
          </div>

          {/* Visual Preview */}
          <div className="bg-slate-800/50 rounded-2xl p-8 border border-purple-500/30">
            <div className="aspect-square bg-gradient-to-br from-purple-900/50 to-pink-900/50 rounded-xl flex items-center justify-center">
              <div className="text-center">
                <div className="text-6xl mb-4">🌐</div>
                <p className="text-slate-400">3D Preview Placeholder</p>
                <p className="text-xs text-slate-500 mt-2">Would render actual shape visualization</p>
              </div>
            </div>
          </div>
        </div>

        {/* What Is It Section */}
        <section className="mb-12 bg-slate-800/30 rounded-2xl p-8 border border-purple-500/20">
          <div className="flex items-center gap-3 mb-4">
            <BookOpen className="text-purple-400" size={24} />
            <h2 className="text-2xl font-bold">What Is It?</h2>
          </div>
          <p className="text-slate-300 leading-relaxed">
            {shape.whatIsIt}
          </p>
        </section>

        {/* Why It Matters */}
        <section className="mb-12">
          <h2 className="text-2xl font-bold mb-6 flex items-center gap-3">
            <Microscope className="text-purple-400" size={24} />
            Why It Matters
          </h2>
          <div className="grid md:grid-cols-2 gap-4">
            {shape.whyItMatters.map((reason, idx) => (
              <div key={idx} className="bg-slate-800/30 rounded-xl p-6 border border-purple-500/20">
                <div className="flex items-start gap-3">
                  <div className="w-6 h-6 rounded-full bg-purple-600 flex items-center justify-center text-sm font-bold flex-shrink-0">
                    {idx + 1}
                  </div>
                  <p className="text-slate-300">{reason}</p>
                </div>
              </div>
            ))}
          </div>
        </section>

        {/* Technical Details */}
        <section className="mb-12 bg-slate-800/30 rounded-2xl p-8 border border-purple-500/20">
          <h2 className="text-2xl font-bold mb-6">Technical Specifications</h2>
          <div className="space-y-6">
            <div>
              <h3 className="text-lg font-semibold text-purple-300 mb-2">Mathematical Equation</h3>
              <code className="block bg-black/50 p-4 rounded-lg text-green-400 font-mono text-sm">
                {shape.technicalDetails.equation}
              </code>
            </div>
            <div>
              <h3 className="text-lg font-semibold text-purple-300 mb-2">Parameters</h3>
              <div className="flex flex-wrap gap-2">
                {shape.technicalDetails.parameters.map((param, idx) => (
                  <span key={idx} className="px-3 py-1 bg-purple-900/50 rounded-full text-sm">
                    {param}
                  </span>
                ))}
              </div>
            </div>
            <div>
              <h3 className="text-lg font-semibold text-purple-300 mb-2">Properties</h3>
              <ul className="space-y-2">
                {shape.technicalDetails.properties.map((prop, idx) => (
                  <li key={idx} className="flex items-center gap-2">
                    <div className="w-1.5 h-1.5 bg-purple-400 rounded-full"></div>
                    <span className="text-slate-300">{prop}</span>
                  </li>
                ))}
              </ul>
            </div>
          </div>
        </section>

        {/* Applications */}
        <section className="mb-12">
          <h2 className="text-2xl font-bold mb-6">Real-World Applications</h2>
          <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-4">
            {shape.applications.map((app, idx) => (
              <div key={idx} className="bg-gradient-to-br from-purple-900/30 to-pink-900/30 rounded-xl p-6 border border-purple-500/30 hover:border-purple-400 transition">
                <p className="text-center font-semibold">{app}</p>
              </div>
            ))}
          </div>
        </section>

        {/* Related Shapes */}
        <section className="mb-12">
          <h2 className="text-2xl font-bold mb-6">Related Shapes</h2>
          <div className="flex flex-wrap gap-3">
            {shape.relatedShapes.map((related, idx) => (
              <a
                key={idx}
                href={`/shapes/${related.toLowerCase().replace(/\s+/g, '_')}`}
                className="px-4 py-2 bg-slate-800 rounded-lg hover:bg-purple-900/50 transition border border-purple-500/30 flex items-center gap-2"
              >
                {related}
                <ExternalLink size={16} />
              </a>
            ))}
          </div>
        </section>

        {/* Research Papers */}
        <section className="bg-slate-800/30 rounded-2xl p-8 border border-purple-500/20">
          <h2 className="text-2xl font-bold mb-6">Research & Further Reading</h2>
          <ul className="space-y-3">
            {shape.researchPapers.map((paper, idx) => (
              <li key={idx}>
                <a href="#" className="flex items-center gap-3 text-purple-300 hover:text-purple-200 transition">
                  <ExternalLink size={18} />
                  <span>{paper}</span>
                </a>
              </li>
            ))}
          </ul>
        </section>
      </main>

      {/* Modal for 3D Tool */}
      {showTool && (
        <div className="fixed inset-0 bg-black/80 backdrop-blur flex items-center justify-center z-50 p-6">
          <div className="bg-slate-900 rounded-2xl border border-purple-500 w-full max-w-6xl max-h-[90vh] overflow-auto">
            <div className="p-6 border-b border-purple-500/30 flex items-center justify-between">
              <h3 className="text-2xl font-bold">{shape.name} Generator</h3>
              <button 
                onClick={() => setShowTool(false)}
                className="p-2 hover:bg-slate-800 rounded-lg transition"
              >
                <X size={24} />
              </button>
            </div>
            <div className="p-6">
              <div className="aspect-video bg-slate-800 rounded-xl flex items-center justify-center">
                <div className="text-center">
                  <div className="text-6xl mb-4">🔧</div>
                  <p className="text-slate-400 text-xl">Your 3D Tool Loads Here</p>
                  <p className="text-slate-500 text-sm mt-2">This would be your actual shape generator interface</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      )}

      {/* Footer */}
      <footer className="border-t border-purple-500/30 bg-black/30 backdrop-blur mt-20">
        <div className="container mx-auto px-6 py-8 text-center text-slate-400 text-sm">
          <p>© 2024 UUON Foundation Inc. | Ωmension Mathematical Universe</p>
          <p className="mt-2">Licensed under CC BY-NC 4.0</p>
        </div>
      </footer>
    </div>
  );
};

export default ShapeLandingPortal;