// ═══════════════════════════════════════════════════
// CRYSTAL MODULE — Complete Package
//
// FILE 1: client/src/lib/crystal.ts
// FILE 2: client/src/hooks/useCrystal.ts  
// FILE 3: Changes to client/src/pages/clouud-terminal.tsx
// FILE 4: server/.replit run command fix (memory)
// FILE 5: server/index.ts connection limiter
//
// Copy each section into the file indicated.
// No new packages needed. Zero dependencies.
// ═══════════════════════════════════════════════════

// ═══════════════════════════════════════════════════
// FILE 1
// client/src/lib/crystal.ts
//
// The crystal engine. Drop this file in as-is.
// Handles all IndexedDB operations.
// ═══════════════════════════════════════════════════

const CRYSTAL_DB_NAME    = “uuon_gcentric”;
const CRYSTAL_DB_VERSION = 1;
const CRYSTAL_STORE      = “crystal”;
const CRYSTAL_KEY        = “device_crystal”;

export interface Crystal {
crystalId: string;           // unique device-session identity
firstContact: string;        // ISO timestamp — when crystal was planted
lastSession: string;         // ISO timestamp — last time device connected
sessionCount: number;        // total sessions from this device
ownerVerified: boolean;      // is this the founder’s device
introShown: boolean;         // has intro been shown — never show again after true
tier: “founder” | “user”;    // device relationship tier
latticeAnchor: number;       // which lattice position (1-33) this device maps to
notes: string;               // optional — any device-specific memory
}

// ── OPEN DATABASE ───────────────────────────────────
function openCrystalDB(): Promise<IDBDatabase> {
return new Promise((resolve, reject) => {
const request = indexedDB.open(CRYSTAL_DB_NAME, CRYSTAL_DB_VERSION);

```
request.onupgradeneeded = (event) => {
  const db = (event.target as IDBOpenDBRequest).result;
  if (!db.objectStoreNames.contains(CRYSTAL_STORE)) {
    db.createObjectStore(CRYSTAL_STORE, { keyPath: "key" });
  }
};

request.onsuccess = () => resolve(request.result);
request.onerror  = () => reject(request.error);
```

});
}

// ── GENERATE CRYSTAL ID ─────────────────────────────
// Not a fingerprint — not browser-dependent.
// A stable UUID generated once and stored forever.
function generateCrystalId(): string {
const timestamp = Date.now().toString(36);
const random    = Math.random().toString(36).slice(2, 10);
const random2   = Math.random().toString(36).slice(2, 10);
return `☧-${timestamp}-${random}-${random2}`;
}

// ── MAP DEVICE TO LATTICE POSITION ──────────────────
// Each device gets a lattice anchor based on its crystal ID.
// Deterministic — same ID always maps to same position.
function deriveLaticeAnchor(crystalId: string): number {
let hash = 0;
for (let i = 0; i < crystalId.length; i++) {
hash = ((hash << 5) - hash) + crystalId.charCodeAt(i);
hash |= 0;
}
return (Math.abs(hash) % 33) + 1; // positions 1–33
}

// ── PLANT CRYSTAL ───────────────────────────────────
// Creates a new crystal for this device.
// Only called once — ever — unless crystal is destroyed.
export async function plantCrystal(
overrides: Partial<Crystal> = {}
): Promise<Crystal> {
const db  = await openCrystalDB();
const id  = generateCrystalId();
const now = new Date().toISOString();

const crystal: Crystal = {
crystalId:     id,
firstContact:  now,
lastSession:   now,
sessionCount:  1,
ownerVerified: false,
introShown:    false,
tier:          “user”,
latticeAnchor: deriveLaticeAnchor(id),
notes:         “”,
…overrides,
};

return new Promise((resolve, reject) => {
const tx      = db.transaction(CRYSTAL_STORE, “readwrite”);
const store   = tx.objectStore(CRYSTAL_STORE);
const request = store.put({ key: CRYSTAL_KEY, …crystal });

```
request.onsuccess = () => resolve(crystal);
request.onerror   = () => reject(request.error);
```

});
}

// ── READ CRYSTAL ────────────────────────────────────
export async function getCrystal(): Promise<Crystal | null> {
try {
const db = await openCrystalDB();

```
return new Promise((resolve, reject) => {
  const tx      = db.transaction(CRYSTAL_STORE, "readonly");
  const store   = tx.objectStore(CRYSTAL_STORE);
  const request = store.get(CRYSTAL_KEY);

  request.onsuccess = () => {
    const result = request.result;
    resolve(result ? { ...result, key: undefined } as Crystal : null);
  };
  request.onerror = () => reject(request.error);
});
```

} catch {
return null;
}
}

// ── UPDATE CRYSTAL ──────────────────────────────────
export async function updateCrystal(
updates: Partial<Crystal>
): Promise<Crystal | null> {
const existing = await getCrystal();
if (!existing) return null;

const db  = await openCrystalDB();
const now = new Date().toISOString();

const updated: Crystal = {
…existing,
…updates,
lastSession:  now,
sessionCount: existing.sessionCount + (updates.sessionCount ? 0 : 1),
};

return new Promise((resolve, reject) => {
const tx      = db.transaction(CRYSTAL_STORE, “readwrite”);
const store   = tx.objectStore(CRYSTAL_STORE);
const request = store.put({ key: CRYSTAL_KEY, …updated });

```
request.onsuccess = () => resolve(updated);
request.onerror   = () => reject(request.error);
```

});
}

// ── GET OR PLANT ────────────────────────────────────
// The main function. Call this on app startup.
// Returns existing crystal or plants a new one.
export async function getOrPlantCrystal(): Promise<{
crystal: Crystal;
isNew: boolean;
}> {
const existing = await getCrystal();

if (existing) {
// Update session info but don’t reset anything
const updated = await updateCrystal({ lastSession: new Date().toISOString() });
return { crystal: updated ?? existing, isNew: false };
}

const fresh = await plantCrystal();
return { crystal: fresh, isNew: true };
}

// ── MARK INTRO SHOWN ────────────────────────────────
export async function markIntroShown(): Promise<void> {
await updateCrystal({ introShown: true });
}

// ── VERIFY OWNER ────────────────────────────────────
// Call this after owner authentication succeeds.
export async function verifyOwner(): Promise<void> {
await updateCrystal({ ownerVerified: true, tier: “founder” });
}

// ── DESTROY CRYSTAL ─────────────────────────────────
// Nuclear option. Only call if user explicitly requests.
export async function destroyCrystal(): Promise<void> {
const db = await openCrystalDB();

return new Promise((resolve, reject) => {
const tx      = db.transaction(CRYSTAL_STORE, “readwrite”);
const store   = tx.objectStore(CRYSTAL_STORE);
const request = store.delete(CRYSTAL_KEY);

```
request.onsuccess = () => resolve();
request.onerror   = () => reject(request.error);
```

});
}

// ── CRYSTAL STATUS ──────────────────────────────────
export function crystalStatus(crystal: Crystal): string {
const age = Math.floor(
(Date.now() - new Date(crystal.firstContact).getTime()) / (1000 * 60 * 60 * 24)
);
return [
`☧ Crystal: ${crystal.crystalId.slice(0, 20)}...`,
`  Tier: ${crystal.tier}`,
`  Age: ${age} days`,
`  Sessions: ${crystal.sessionCount}`,
`  Lattice anchor: position ${crystal.latticeAnchor}`,
`  Owner: ${crystal.ownerVerified ? "verified" : "unverified"}`,
].join(”\n”);
}

// ═══════════════════════════════════════════════════
// FILE 2
// client/src/hooks/useCrystal.ts
//
// React hook. Use this in any component.
// ═══════════════════════════════════════════════════

/*
import { useState, useEffect } from “react”;
import { getOrPlantCrystal, markIntroShown, Crystal } from “../lib/crystal”;

interface UseCrystalReturn {
crystal: Crystal | null;
isNew: boolean;
loading: boolean;
shouldShowIntro: boolean;
dismissIntro: () => Promise<void>;
}

export function useCrystal(): UseCrystalReturn {
const [crystal, setCrystal]           = useState<Crystal | null>(null);
const [isNew, setIsNew]               = useState(false);
const [loading, setLoading]           = useState(true);
const [shouldShowIntro, setShowIntro] = useState(false);

useEffect(() => {
getOrPlantCrystal().then(({ crystal, isNew }) => {
setCrystal(crystal);
setIsNew(isNew);
setShowIntro(!crystal.introShown);
setLoading(false);

```
  // Log crystal status to console (founder diagnostic)
  if (crystal.tier === "founder") {
    console.log(crystalStatus(crystal));
  }
});
```

}, []);

const dismissIntro = async () => {
await markIntroShown();
setShowIntro(false);
};

return { crystal, isNew, loading, shouldShowIntro, dismissIntro };
}
*/

// ═══════════════════════════════════════════════════
// FILE 3
// client/src/pages/clouud-terminal.tsx
//
// Add these changes to your existing file.
// Find each comment and add the line below it.
// ═══════════════════════════════════════════════════

/*
// ── ADD AT TOP (with other imports) ─────────────────
import { useCrystal } from “../hooks/useCrystal”;

// ── ADD INSIDE COMPONENT (near top of function body) ─
const { crystal, loading, shouldShowIntro, dismissIntro } = useCrystal();

// ── REPLACE YOUR EXISTING INTRO RENDER with this ────
// This is the key fix — intro only shows if crystal says so

if (loading) return null; // wait for crystal check

// ── WRAP YOUR EXISTING INTRO JSX like this ──────────
{shouldShowIntro && (
<YourExistingIntroComponent
onComplete={async () => {
await dismissIntro();
// your existing onComplete logic
}}
/>
)}

// ── IN YOUR DEVELOPER/METRICS PANEL ─────────────────
// Show crystal status to founder only
{crystal?.tier === “founder” && (

  <div className="crystal-status font-mono text-xs text-green-400 mt-2">
    <div>☧ Crystal: {crystal.crystalId.slice(0, 16)}...</div>
    <div>Sessions: {crystal.sessionCount} · Anchor: L{crystal.latticeAnchor}</div>
    <div>Age: {Math.floor((Date.now() - new Date(crystal.firstContact).getTime()) / 86400000)}d</div>
  </div>
)}
*/

// ═══════════════════════════════════════════════════
// FILE 4
// .replit — memory fix
//
// Find your run command and replace it.
// This skips the build step — saves ~400MB on startup.
// ═══════════════════════════════════════════════════

/*
// FIND THIS (or similar):
run = “npm run build && node dist/index.js”

// REPLACE WITH:
run = “npx tsx server/index.ts”

// If tsx is not installed, add to server/package.json devDependencies:
// “tsx”: “^4.0.0”
// Then: npm install
*/

// ═══════════════════════════════════════════════════
// FILE 5
// server/index.ts — connection limiter
//
// Prevents 3 tabs hammering /api/conversations at once.
// Add this block right after your express() setup.
// ═══════════════════════════════════════════════════

/*
import { Request, Response, NextFunction } from “express”;

// ── CRYSTAL-AWARE CONNECTION LIMITER ────────────────
// If the same device opens 3 tabs, only the first
// gets a fresh /api/conversations fetch.
// Others get a 429 with a “use crystal cache” hint.

const recentRequests = new Map<string, number>();
const WINDOW_MS      = 2000; // 2 second window
const MAX_PER_WINDOW = 1;    // 1 conversations fetch per 2 seconds per IP

app.use(”/api/conversations”, (req: Request, res: Response, next: NextFunction) => {
const ip  = req.ip ?? “unknown”;
const now = Date.now();
const last = recentRequests.get(ip) ?? 0;

if (now - last < WINDOW_MS) {
// Too fast — likely multiple tabs
return res.status(429).json({
error:  “duplicate_tab”,
message: “Crystal cache is current. No fetch needed.”,
hint:   “Use crystal.lastSession to determine if refresh is needed.”,
});
}

recentRequests.set(ip, now);

// Clean up old entries every 100 requests
if (recentRequests.size > 100) {
for (const [key, time] of recentRequests) {
if (now - time > WINDOW_MS * 10) recentRequests.delete(key);
}
}

next();
});
*/

// ═══════════════════════════════════════════════════
// SUMMARY — WHAT EACH FILE DOES
// ═══════════════════════════════════════════════════

/*
crystal.ts          → The crystal engine. IndexedDB. No expiry. No corporate tracking.
getOrPlantCrystal() — call on startup, always.
markIntroShown()    — call after intro completes.
verifyOwner()       — call after founder auth.
destroyCrystal()    — user-controlled nuclear option only.

useCrystal.ts       → React hook. shouldShowIntro is your intro gate.
One line in your component. Done.

clouud-terminal.tsx → Three additions. Import hook. Check shouldShowIntro.
Show crystal status in metrics panel for founder only.

.replit             → Change run command. Skip build step. Save 400MB.
Single most impactful memory fix available.

server/index.ts     → Rate limit /api/conversations to 1 per 2 seconds per IP.
Kills the 3-tab hammering problem at the source.

RESULT:

- Intro never shows twice on the same device
- Memory spike on startup eliminated
- 3-tab OOM kills stopped
- Each device has a permanent lattice anchor
- Founder device shows crystal diagnostics
- No cookies. No corporate tracking. No expiry.
- Crystal lives until the user deliberately destroys it.
  */