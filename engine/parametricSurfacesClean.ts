import { SurfaceParameters } from '../types/math';
import { ParametricSurface } from '../types/shapes';
import { applyPlaneTransform } from './planeTransforms';
import { UNIFIED_SHAPES } from './unifiedShapes';
import { CLEAN_SURFACES } from './cleanMathEngine';
import { EXCLUSIVE_SHAPES } from './exclusiveShapes';
import { NON_EUCLIDEAN_SHAPES } from './nonEuclideanShapes';
import { RIEMANN_SURFACES } from './riemannSurfaces';
import { EDUCATIONAL_SURFACES } from './educationalSurfaces';
import { GENERATIVE_ALGORITHMS } from './generativeAlgorithms';
import { NOISE_FUNCTIONS } from './noiseFunctions';
import { DIFFERENTIAL_GROWTH } from './differentialGrowth';
import { ATTRACTOR_SYSTEMS } from './attractorSystems';
import { VORONOI_SYSTEMS } from './voronoiSystems';
import { QUANTUM_PARAMETRIC_FUNCTIONS } from './quantumParametricFunctions';
import { QUANTUM_GAP_SURFACES } from './quantumGapMathematics';
import { DNA_STRUCTURES } from './dnaStructures';
import { HUMAN_ANATOMY_SHAPES } from './humanAnatomyShapes';
import { HYPERCOMPUTATION_SURFACES } from './hypercomputationSurfaces';
import { ASTROPHYSICAL_PHENOMENA } from './astrophysicalPhenomena';
import { HISTORICAL_ALGORITHMS } from './historicalAlgorithms';
import { PROTEIN_STRUCTURES } from './proteinStructures';
import { POLYMER_CHAINS } from './polymerChains';
import { TISSUE_STRUCTURES } from './tissueStructures';
import { EXTENDED_CRYSTALS } from './extendedCrystals';
import { ASTRONOMICAL_OBJECTS } from './astronomicalObjects';
import { WEATHER_SYSTEMS } from './weatherSystems';
import { MECHANICAL_SHAPES } from './mechanicalShapes';
import { FINANCIAL_MATHEMATICS } from './financialMathematics';
import { ADVANCED_PHYSICS_SIMS } from './advancedPhysicsSims';
import { CONSCIOUSNESS_THEORY } from './consciousnessTheory';
import { MATHEMATICAL_CONSTANTS } from './mathematicalConstants';
import { FOUR_DIMENSIONAL_SHAPES } from './fourDimensionalShapes';
import { FIVE_DIMENSIONAL_SHAPES, HIGHER_DIMENSIONAL_SHAPES } from './higherDimensionalShapes';
import { NATIONAL_MOTTO_ALGORITHMS } from './nationalMottoAlgorithms';
import { 
  CONSENSUS_ALGORITHMS, 
  CRYPTOGRAPHIC_ALGORITHMS, 
  PROOF_SYSTEMS, 
  LAYER2_ALGORITHMS, 
  PRIVACY_ALGORITHMS, 
  POST_QUANTUM_ALGORITHMS 
} from './blockchainAlgorithmsEngine';
import { INVERSE_FISHBOWL_SPACE } from './inverseFishBowlSpace';
import { HYDROGEN_ORBITALS } from './hydrogenOrbitals';
import { NEURAL_LATTICE_ALGORITHMS } from './neuralLatticeAlgorithms';
import { QUANTUM_COMPUTING_ALGORITHMS } from './quantumComputingAlgorithms';
import { DUAL_MIRROR_ENERGY_SYSTEM } from './dualMirrorEnergySystem';
import { AI_ML_ALGORITHMS } from './aiMlAlgorithmsLibrary';
import { BIOLOGICAL_SHAPE_IMPLEMENTATIONS, MATHEMATICAL_SHAPE_IMPLEMENTATIONS } from './biologicalShapeImplementations';
import { TOPOLOGY_KNOTS } from './topologyKnotsFixed';
import { TOPOLOGY_DIFFERENTIAL_SHAPES } from './topologyDifferentialShapes';
import { CATEGORY_THEORY } from './categoryTheory';
import { GROUP_THEORY } from './groupTheory';
import { UNIFIED_MATH_SYMBOLS, TRIG_WAVE_FUNCTIONS } from './unifiedMathSymbols';
import { UNIVERSAL_MATHEMATICS } from './universalMathematics';
import { MULTIDIMENSIONAL_FRACTALS } from './multidimensionalFractals';
import { SACRED_GEOMETRY } from './sacredGeometry';
import { ADVANCED_TOPOLOGICAL_SURFACES } from './advancedTopologicalSurfaces';
import { REAL_WORLD_OBJECTS } from './realWorldObjects';
import { NON_EUCLIDEAN_GEOMETRIES } from './nonEuclideanGeometries';
import { SEQUENCE_PATTERNS } from './sequencePatterns';
import { ADVANCED_PHYSICS_EQUATIONS } from './advancedPhysicsEquations';
import { SCHRODINGER_EQUATIONS } from './schrodingerEquations';
import { ENTANGLEMENT_ALGORITHMS } from './entanglementAlgorithms';
import { CHAKRA_SHAPES } from './chakraShapes';
import { BABYLONIAN_ZODIAC_SHAPES } from './babylonianZodiacShapes';
import { GENERAL_RELATIVITY_SHAPES } from './generalRelativityShapes';
import { QUANTUM_GRAVITY_EQUATIONS } from './quantumGravityEquations';
import { THEORY_OF_EVERYTHING_SHAPES } from './theoryOfEverythingShapes';
import { TEN_PERCENT_SHAPES } from './tenPercentShapes';
import { SET_THEORY_SHAPES } from './setTheoryShapes';
import { ENTROPIC_PRINCIPLES } from './entropicPrinciples';
import { 
  SCIENTIFIC_EXPANSION_SHAPES, 
  MOLECULAR_BIOLOGY_SHAPES,
  MICROBIOLOGY_SHAPES,
  BOTANY_SHAPES,
  ZOOLOGY_ECOLOGY_SHAPES,
  GEOLOGY_SHAPES,
  OCEANOGRAPHY_SHAPES,
  METEOROLOGY_SHAPES,
  ECONOMICS_SHAPES,
  SOCIOLOGY_SHAPES,
  POLITICAL_SCIENCE_SHAPES,
  INDUSTRIAL_ENGINEERING_SHAPES,
  CIVIL_ENGINEERING_SHAPES,
  AEROSPACE_ENGINEERING_SHAPES,
  BIOINFORMATICS_ALGORITHMS,
  UNIFIED_WHOLENESS
} from './scientificExpansionShapes';
import { EARTH_SCIENCES_SHAPES } from './earthSciencesShapes';
import { LIFE_SCIENCES_SHAPES } from './lifeSciencesShapes';
import { COSMIC_PHYSICS_EQUATIONS } from './cosmicPhysicsEquations';
import { COSMIC_HISTORY_GAPS } from './cosmicHistoryGaps';
import { 
  CORE_MATH_MODULO,
  CRYPTO_MODULO,
  CS_DATA_STRUCTURES_MODULO,
  SEQUENCES_PATTERNS_MODULO,
  GRAPHICS_SIMULATION_MODULO,
  GEOMETRY_SPATIAL_MODULO,
  AUDIO_SIGNAL_MODULO,
  MODULO_ALGORITHMS
} from './moduloAlgorithms';
import { 
  AI_ML_MODULO,
  NETWORKING_MODULO,
  OS_LOWLEVEL_MODULO,
  ROBOTICS_PHYSICS_MODULO,
  CHAOS_FRACTALS_MODULO,
  COSMOLOGY_MODULO,
  UUON_CUSTOM_MODULO,
  MODULO_ALGORITHMS_PART2
} from './moduloAlgorithmsPart2';
import { GMOD6_SURFACES } from './uuon-gmod6-engine';
import { GENERAL_RELATIVITY_FALLBACKS, TOPOLOGY_FALLBACKS, MISSING_SHAPES_BRIDGE } from './missingShapesBridge';
import { COMPLETE_MISSING_SHAPES } from './completeMissingShapesLibrary';
import { TENSOR_ALGEBRA_SHAPES } from './tensorAlgebraEngine';
import { FIELD_THEORY_SHAPES } from './fieldTheoryEngine';
import { WAVE_ALGORITHMS_SHAPES } from './waveAlgorithmsEngine';
import { SOCIAL_SCIENCES_SHAPES } from './socialSciencesShapes';
import { FRACTAL_ANALYSIS_SHAPES } from './fractalAnalysisShapes';
import { QPU_QUANTUM_COMPUTING_SHAPES } from './qpuQuantumComputingShapes';
import { UNIFIED_MASTER_EQUATION_SHAPES } from './unifiedMasterEquation';
import { QUANTUM_VISUALIZATION_SHAPES } from './quantumVisualizationShapes';
import { FOUR_DIMENSIONAL_4D_SHAPES } from './fourDimensional4DShapes'; // Import new 4D shapes library
import { BASIC_GEOMETRY_FORMULAS } from './basicGeometryFormulas';
import { TIME_TRAVEL_PHYSICS } from './timeTravelPhysics';
import { INVERSE_SQUARE_LAW_SHAPES } from './inverseSquareLawShapes';
import { RARE_MANIFOLDS } from './rareManifolds';

export { NEURAL_LATTICE_ALGORITHMS };
export { QUANTUM_COMPUTING_ALGORITHMS };
export { DUAL_MIRROR_ENERGY_SYSTEM };
export { AI_ML_ALGORITHMS };
export { BIOLOGICAL_SHAPE_IMPLEMENTATIONS };
export { CLEAN_SURFACES };
export { EXCLUSIVE_SHAPES };
export { NON_EUCLIDEAN_SHAPES };
export { RIEMANN_SURFACES };
export { EDUCATIONAL_SURFACES };
export { HISTORICAL_ALGORITHMS };
export { MATHEMATICAL_CONSTANTS };
export { QUANTUM_PARAMETRIC_FUNCTIONS };
export { MECHANICAL_SHAPES };
export { WEATHER_SYSTEMS };
export { ASTRONOMICAL_OBJECTS };
export { CONSCIOUSNESS_THEORY };
export { EXTENDED_CRYSTALS };
export { ADVANCED_PHYSICS_SIMS };
export { HYDROGEN_ORBITALS };
export { QUANTUM_GAP_SURFACES };
export { NOISE_FUNCTIONS };
export { DIFFERENTIAL_GROWTH };
export { PROTEIN_STRUCTURES };
export { ASTROPHYSICAL_PHENOMENA };
export { VORONOI_SYSTEMS };
export { HYPERCOMPUTATION_SURFACES };
export { NATIONAL_MOTTO_ALGORITHMS };
export { HUMAN_ANATOMY_SHAPES };
export { ATTRACTOR_SYSTEMS };
export { POLYMER_CHAINS };
export { TISSUE_STRUCTURES };
export { GENERATIVE_ALGORITHMS };
export { FINANCIAL_MATHEMATICS };
export { DNA_STRUCTURES };
export { FOUR_DIMENSIONAL_SHAPES };
export { UNIFIED_SHAPES };
export { TOPOLOGY_KNOTS };
export { TOPOLOGY_DIFFERENTIAL_SHAPES };
export { CATEGORY_THEORY };
export { GROUP_THEORY };
export { UNIFIED_MATH_SYMBOLS };
export { UNIVERSAL_MATHEMATICS };
export { MULTIDIMENSIONAL_FRACTALS };
export { SACRED_GEOMETRY };
export { ADVANCED_TOPOLOGICAL_SURFACES };
export { REAL_WORLD_OBJECTS };
export { NON_EUCLIDEAN_GEOMETRIES };
export { SEQUENCE_PATTERNS };
export { ADVANCED_PHYSICS_EQUATIONS };
export { SCHRODINGER_EQUATIONS };
export { ENTANGLEMENT_ALGORITHMS };
export { CHAKRA_SHAPES };
export { GENERAL_RELATIVITY_SHAPES };
export { QUANTUM_GRAVITY_EQUATIONS };
export { THEORY_OF_EVERYTHING_SHAPES };
export { TEN_PERCENT_SHAPES };
export { SET_THEORY_SHAPES };
export { ENTROPIC_PRINCIPLES };
export { SCIENTIFIC_EXPANSION_SHAPES };
export { MOLECULAR_BIOLOGY_SHAPES };
export { EARTH_SCIENCES_SHAPES };
export { LIFE_SCIENCES_SHAPES };
export { INVERSE_SQUARE_LAW_SHAPES };
export { RARE_MANIFOLDS };

export type { ParametricSurface } from '../types/shapes';

export const PARAMETRIC_SURFACES: Record<string, ParametricSurface> = {
  // RARE MANIFOLDS — highest-priority: appear first
  ...RARE_MANIFOLDS,
  ...FINANCIAL_MATHEMATICS,
  ...ADVANCED_PHYSICS_SIMS,
  ...CONSCIOUSNESS_THEORY,
  ...MATHEMATICAL_CONSTANTS,
  // 4D HYPERDIMENSIONAL SHAPES
  ...FOUR_DIMENSIONAL_SHAPES,
  ...FOUR_DIMENSIONAL_4D_SHAPES, // Add new 4D shapes
  // 5D AND HIGHER DIMENSIONAL SHAPES
  ...FIVE_DIMENSIONAL_SHAPES,
  ...HIGHER_DIMENSIONAL_SHAPES,
  ...INVERSE_FISHBOWL_SPACE,
  ...HYDROGEN_ORBITALS,
  ...NEURAL_LATTICE_ALGORITHMS,
  ...QUANTUM_COMPUTING_ALGORITHMS,
  ...DUAL_MIRROR_ENERGY_SYSTEM,
  ...AI_ML_ALGORITHMS,
  ...QUANTUM_PARAMETRIC_FUNCTIONS, // Include new quantum entities
  ...COSMIC_PHYSICS_EQUATIONS, // Add cosmic physics equations

  // Quantum Gravity & Planck Scale Physics
  ...QUANTUM_GRAVITY_EQUATIONS,
  ...Object.fromEntries(CONSENSUS_ALGORITHMS.map(algo => [algo.id, algo])),
  ...Object.fromEntries(CRYPTOGRAPHIC_ALGORITHMS.map(algo => [algo.id, algo])),
  ...Object.fromEntries(PROOF_SYSTEMS.map(algo => [algo.id, algo])),
  ...Object.fromEntries(LAYER2_ALGORITHMS.map(algo => [algo.id, algo])),
  ...Object.fromEntries(PRIVACY_ALGORITHMS.map(algo => [algo.id, algo])),
  ...Object.fromEntries(POST_QUANTUM_ALGORITHMS.map(algo => [algo.id, algo])),
  // BASIC GEOMETRY FORMULAS - 25 Essential Shapes
  ...BASIC_GEOMETRY_FORMULAS,
  ...TIME_TRAVEL_PHYSICS,
  // INVERSE SQUARE LAW SHAPES - Universal 1/r² field equations
  ...INVERSE_SQUARE_LAW_SHAPES,
  
  // BASIC GEOMETRIC SHAPES - Essential Foundation
  square: {
    name: "Square",
    equation: (u, v, params) => {
      const { a = 2, b = 2, c = 0 } = params; // a=width, b=height, c=z-offset
      return [a * (u - 0.5), b * (v - 0.5), c];
    },
    defaultParams: { a: 2, b: 2, c: 0, uMin: 0, uMax: 1, vMin: 0, vMax: 1, uSegments: 20, vSegments: 20 }
  },

  cube: {
    name: "Cube",
    equation: (u, v, params) => {
      const { a = 1, b = 1, c = 1 } = params; // Use a, b, c for width, height, depth - equal for cube
      const face = Math.floor(u * 6) % 6;
      const localU = (u * 6) % 1;
      const localV = v;

      const positions = [
        [a * (localU - 0.5), b * (localV - 0.5), c * 0.5],   // front
        [a * (localU - 0.5), b * (localV - 0.5), -c * 0.5],  // back
        [a * 0.5, b * (localU - 0.5), c * (localV - 0.5)],   // right
        [-a * 0.5, b * (localU - 0.5), c * (localV - 0.5)],  // left
        [a * (localU - 0.5), b * 0.5, c * (localV - 0.5)],   // top
        [a * (localU - 0.5), -b * 0.5, c * (localV - 0.5)]   // bottom
      ];

      return positions[face] as [number, number, number];
    },
    defaultParams: { a: 1, b: 1, c: 1, uMin: 0, uMax: 1, vMin: 0, vMax: 1, uSegments: 48, vSegments: 48 }
  },

  circle: {
    name: "Circle",
    equation: (u, v, params) => {
      const { a = 2, b = 2, c = 0 } = params; // a=x-radius, b=y-radius (ellipse), c=z-offset
      const theta = u * 2 * Math.PI;
      const r = v; // 0 to 1 from center to edge
      return [a * r * Math.cos(theta), b * r * Math.sin(theta), c];
    },
    defaultParams: { a: 2, b: 2, c: 0, uMin: 0, uMax: 1, vMin: 0, vMax: 1, uSegments: 64, vSegments: 16 }
  },

  triangle: {
    name: "Triangle",
    equation: (u, v, params) => {
      const { a = 2, b = 2, c = 0 } = params; // a=base width, b=height, c=z-offset
      if (u + v <= 1) {
        const x = a * (u - 0.5);
        const y = b * v;
        return [x, y, c];
      }
      return [0, 0, c]; // Outside triangle bounds
    },
    defaultParams: { a: 2, b: 2, c: 0, uMin: 0, uMax: 1, vMin: 0, vMax: 1, uSegments: 24, vSegments: 24 }
  },

  cylinder: {
    name: "Cylinder",
    equation: (u, v, params) => {
      const { a = 2, b = 2, c = 2 } = params; // a=x-radius, b=y-radius (elliptical), c=height
      const theta = u * 2 * Math.PI;
      const height = c * (v - 0.5);
      return [a * Math.cos(theta), b * Math.sin(theta), height];
    },
    defaultParams: { a: 2, b: 2, c: 2, uMin: 0, uMax: 1, vMin: 0, vMax: 1, uSegments: 48, vSegments: 24 }
  },

  torus: {
    name: "Torus",
    equation: (u, v, params) => {
      const { a = 1, b = 0.5, c = 1 } = params; // a=major radius, b=tube radius, c=vertical stretch
      const theta = u * 2 * Math.PI;
      const phi = v * 2 * Math.PI;
      const x = (a + b * Math.cos(phi)) * Math.cos(theta);
      const y = (a + b * Math.cos(phi)) * Math.sin(theta);
      const z = c * b * Math.sin(phi);
      return [x, y, z];
    },
    defaultParams: { a: 1, b: 0.5, c: 1, uMin: 0, uMax: 1, vMin: 0, vMax: 1, uSegments: 48, vSegments: 24 }
  },

  tetrahedron: {
    name: "Tetrahedron",
    equation: (u, v, params) => {
      const { a = 2, b = 2, c = 2 } = params; // a=x-scale, b=y-scale, c=z-scale
      const face = Math.floor(u * 4) % 4;
      const localU = (u * 4) % 1;
      const localV = v;

      const vertices = [
        [a, b, c], [-a, -b, c], [-a, b, -c], [a, -b, -c]
      ];
      const faces = [[0,1,2], [0,1,3], [0,2,3], [1,2,3]];
      const faceVerts = faces[face].map(i => vertices[i]);

      if (localU + localV <= 1) {
        return [
          faceVerts[0][0] * (1 - localU - localV) + faceVerts[1][0] * localU + faceVerts[2][0] * localV,
          faceVerts[0][1] * (1 - localU - localV) + faceVerts[1][1] * localU + faceVerts[2][1] * localV,
          faceVerts[0][2] * (1 - localU - localV) + faceVerts[1][2] * localU + faceVerts[2][2] * localV
        ];
      }
      return [0, 0, 0];
    },
    defaultParams: { a: 2, b: 2, c: 2, uMin: 0, uMax: 1, vMin: 0, vMax: 1, uSegments: 24, vSegments: 24 }
  },

  cone: {
    name: "Cone",
    equation: (u, v, params) => {
      const { a = 2, b = 2, c = 2 } = params; // a=x-radius, b=y-radius (elliptical base), c=height
      const theta = u * 2 * Math.PI;
      const radius = 1 - v; // Taper from base to tip
      const height = c * v;
      return [a * radius * Math.cos(theta), b * radius * Math.sin(theta), height];
    },
    defaultParams: { a: 2, b: 2, c: 2, uMin: 0, uMax: 1, vMin: 0, vMax: 1, uSegments: 48, vSegments: 24 }
  },

  sphere: {
    name: "Sphere",
    equation: (u, v, params) => {
      const { a = 2, b = 2, c = 2 } = params; // a=x-radius, b=y-radius, c=z-radius (ellipsoid)
      const theta = u; // longitude
      const phi = v;   // latitude

      const x = a * Math.sin(phi) * Math.cos(theta);
      const y = b * Math.sin(phi) * Math.sin(theta);
      const z = c * Math.cos(phi);

      return [x, y, z];
    },
    defaultParams: { a: 2, b: 2, c: 2, uMin: 0, uMax: 2 * Math.PI, vMin: 0, vMax: Math.PI, uSegments: 64, vSegments: 32 }
  },

  // STREAMLINED HIGH-VALUE COMMERCIAL SURFACES
  nautilus_shell: {
    name: "🐚 Nautilus Shell - Streamlined",
    equation: (u, v, params) => {
      const { a = 2.0, b = 0.3, c = 1.618, d = 1.0 } = params;
      const theta = u;
      const phi = v;

      // Core logarithmic spiral
      const radius = a * Math.exp(b * theta);
      const chamber = c * Math.sin(d * theta);

      const x = (radius + chamber) * Math.cos(theta);
      const y = (radius + chamber) * Math.sin(theta);
      const z = phi * 2 + Math.sin(theta * 2);

      return [x, y, z];
    },
    defaultParams: { 
      a: 2.0, b: 0.3, c: 1.618, d: 1.0,
      uMin: 0, uMax: 4 * Math.PI, vMin: 0, vMax: 2 * Math.PI, uSegments: 100, vSegments: 50 
    }
  },

  enneper_surface: {
    name: "🌀 Enneper's Surface - Streamlined", 
    equation: (u, v, params) => {
      const { a = 1.0, b = 1.0, c = 1.0, d = 0.3 } = params;

      // Core Enneper equations
      const x = a * (u - u*u*u/3 + u*v*v) + d * Math.sin(u);
      const y = b * (v - v*v*v/3 + u*u*v) + d * Math.cos(v);
      const z = c * (u*u - v*v);

      return [x, y, z];
    },
    defaultParams: { 
      a: 1.0, b: 1.0, c: 1.0, d: 0.3,
      uMin: -2, uMax: 2, vMin: -2, vMax: 2, uSegments: 80, vSegments: 80 
    }
  },

  flower_of_life_3d: {
    name: "🌸 3D Flower of Life - Streamlined",
    equation: (u, v, params) => {
      const { a = 1.0, b = 1.0, c = 6.0, d = 0.618 } = params;

      const circleIndex = Math.floor(u * c) % Math.floor(c);
      const theta = (u * c) % 1 * 2 * Math.PI;
      const phi = v * 2 * Math.PI;

      // Hexagonal pattern
      let centerX = 0, centerY = 0;
      if (circleIndex > 0) {
        const hexAngle = (circleIndex - 1) * Math.PI / 3;
        centerX = a * Math.cos(hexAngle);
        centerY = a * Math.sin(hexAngle);
      }

      const x = centerX + b * Math.cos(theta);
      const y = centerY + b * Math.sin(theta);
      const z = d * Math.sin(phi) + Math.cos(theta * 6);

      return [x, y, z];
    },
    defaultParams: { 
      a: 1.0, b: 1.0, c: 6.0, d: 0.618,
      uMin: 0, uMax: 2 * Math.PI, vMin: 0, vMax: 2 * Math.PI, uSegments: 120, vSegments: 60 
    }
  },

  catenoid: {
    name: "⏳ Catenoid - Streamlined",
    equation: (u, v, params) => {
      const { a = 1.0, b = 1.0, c = 0.5, d = 1.0 } = params;

      const theta = u;
      const w = v;
      const radius = a * Math.cosh(b * w) + c * Math.sin(d * theta);

      const x = radius * Math.cos(theta);
      const y = radius * Math.sin(theta); 
      const z = w;

      return [x, y, z];
    },
    defaultParams: { 
      a: 1.0, b: 1.0, c: 0.5, d: 1.0,
      uMin: 0, uMax: 2 * Math.PI, vMin: -2, vMax: 2, uSegments: 60, vSegments: 100 
    }
  },

  helicoid: {
    name: "🌪️ Helicoid - Streamlined",
    equation: (u, v, params) => {
      const { a = 0.5, b = 1.0, c = 1.0, d = 0.3 } = params;

      const rho = u;
      const theta = v;

      const x = (rho + c * Math.sin(d * theta)) * Math.cos(theta);
      const y = (rho + c * Math.sin(d * theta)) * Math.sin(theta);
      const z = a * theta + b * Math.sin(rho);

      return [x, y, z];
    },
    defaultParams: { 
      a: 0.5, b: 1.0, c: 1.0, d: 0.3,
      uMin: 0, uMax: 2 * Math.PI, vMin: -2, vMax: 2, uSegments: 80, vSegments: 100 
    }
  }
};

// PLANE TRANSFORM INTEGRATION
export function generateSurfaceWithPlaneTransform(
  surfaceType: string, 
  planeTransform: string | null,
  params: SurfaceParameters
): [number, number, number][] {
  const surface = PARAMETRIC_SURFACES[surfaceType];
  if (!surface) return [];

  const { uMin = 0, uMax = 1, vMin = 0, vMax = 1, uSegments = 32, vSegments = 32 } = surface.defaultParams;
  const points: [number, number, number][] = [];

  // Generate base surface points
  for (let i = 0; i <= uSegments; i++) {
    for (let j = 0; j <= vSegments; j++) {
      const u = uMin + (uMax - uMin) * i / uSegments;
      const v = vMin + (vMax - vMin) * j / vSegments;
      const point = surface.equation(u, v, params);
      points.push(point);
    }
  }

  // Apply plane transformation if specified
  if (planeTransform && planeTransform !== 'none') {
    return applyPlaneTransform(points, planeTransform, params);
  }

  return points;
}

export function getSurfaceEquation(type: string): ParametricSurface | null {
  try {
    // Search all available shape collections for the requested type
    // Priority order: Historical, New scientific categories, Quantum Gap, UNIFIED_SHAPES, Astrophysical, DNA, Human Anatomy, etc.
    const allCollections: Array<Record<string, any>> = [
      // Priority 0: Ancient historical mathematical reconstructions
      BABYLONIAN_ZODIAC_SHAPES,

      // Priority 1: Core physics and scientific libraries
      GENERAL_RELATIVITY_SHAPES,
      QUANTUM_GRAVITY_EQUATIONS,
      ENTROPIC_PRINCIPLES,
      THEORY_OF_EVERYTHING_SHAPES,
      TEN_PERCENT_SHAPES,
      ADVANCED_PHYSICS_EQUATIONS,
      SCHRODINGER_EQUATIONS,
      COSMIC_PHYSICS_EQUATIONS,
      COSMIC_HISTORY_GAPS,
      FIELD_THEORY_SHAPES,
      TENSOR_ALGEBRA_SHAPES,
      WAVE_ALGORITHMS_SHAPES,
      UNIFIED_MASTER_EQUATION_SHAPES,

      // Priority 2: Mathematics and topology
      SET_THEORY_SHAPES,
      CATEGORY_THEORY,
      GROUP_THEORY,
      TOPOLOGY_KNOTS,
      TOPOLOGY_DIFFERENTIAL_SHAPES,
      UNIFIED_MATH_SYMBOLS,
      UNIVERSAL_MATHEMATICS,
      MULTIDIMENSIONAL_FRACTALS,
      FRACTAL_ANALYSIS_SHAPES,
      SACRED_GEOMETRY,
      ADVANCED_TOPOLOGICAL_SURFACES,
      NON_EUCLIDEAN_GEOMETRIES,
      SEQUENCE_PATTERNS,
      GMOD6_SURFACES,

      // Priority 2b: Modulo algorithms (comprehensive)
      CORE_MATH_MODULO,
      CRYPTO_MODULO,
      CS_DATA_STRUCTURES_MODULO,
      SEQUENCES_PATTERNS_MODULO,
      GRAPHICS_SIMULATION_MODULO,
      GEOMETRY_SPATIAL_MODULO,
      AUDIO_SIGNAL_MODULO,
      MODULO_ALGORITHMS,
      AI_ML_MODULO,
      NETWORKING_MODULO,
      OS_LOWLEVEL_MODULO,
      ROBOTICS_PHYSICS_MODULO,
      CHAOS_FRACTALS_MODULO,
      COSMOLOGY_MODULO,
      UUON_CUSTOM_MODULO,
      MODULO_ALGORITHMS_PART2,

      // Priority 3: AI/ML and quantum computing
      AI_ML_ALGORITHMS,
      NEURAL_LATTICE_ALGORITHMS,
      QUANTUM_COMPUTING_ALGORITHMS,
      QPU_QUANTUM_COMPUTING_SHAPES,
      QUANTUM_VISUALIZATION_SHAPES,
      ENTANGLEMENT_ALGORITHMS,
      QUANTUM_PARAMETRIC_FUNCTIONS,
      QUANTUM_GAP_SURFACES,

      // Priority 4: Biological and life sciences
      SCIENTIFIC_EXPANSION_SHAPES,
      UNIFIED_WHOLENESS,
      MOLECULAR_BIOLOGY_SHAPES,
      MICROBIOLOGY_SHAPES,
      BOTANY_SHAPES,
      ZOOLOGY_ECOLOGY_SHAPES,
      BIOINFORMATICS_ALGORITHMS,
      EARTH_SCIENCES_SHAPES,
      LIFE_SCIENCES_SHAPES,
      BIOLOGICAL_SHAPE_IMPLEMENTATIONS,
      DNA_STRUCTURES,
      PROTEIN_STRUCTURES,
      POLYMER_CHAINS,
      TISSUE_STRUCTURES,
      HUMAN_ANATOMY_SHAPES,

      // Priority 4b: Earth and environmental sciences
      GEOLOGY_SHAPES,
      OCEANOGRAPHY_SHAPES,
      METEOROLOGY_SHAPES,

      // Priority 4c: Social sciences and engineering
      SOCIAL_SCIENCES_SHAPES,
      ECONOMICS_SHAPES,
      SOCIOLOGY_SHAPES,
      POLITICAL_SCIENCE_SHAPES,
      INDUSTRIAL_ENGINEERING_SHAPES,
      CIVIL_ENGINEERING_SHAPES,
      AEROSPACE_ENGINEERING_SHAPES,

      // Priority 5: Cryptography and blockchain (converted from arrays to objects)
      Object.fromEntries(CONSENSUS_ALGORITHMS.map(algo => [algo.id, algo])),
      Object.fromEntries(CRYPTOGRAPHIC_ALGORITHMS.map(algo => [algo.id, algo])),
      Object.fromEntries(PROOF_SYSTEMS.map(algo => [algo.id, algo])),
      Object.fromEntries(LAYER2_ALGORITHMS.map(algo => [algo.id, algo])),
      Object.fromEntries(PRIVACY_ALGORITHMS.map(algo => [algo.id, algo])),
      Object.fromEntries(POST_QUANTUM_ALGORITHMS.map(algo => [algo.id, algo])),

      // Priority 6: Astrophysics and cosmology
      ASTROPHYSICAL_PHENOMENA,
      ASTRONOMICAL_OBJECTS,
      WEATHER_SYSTEMS,

      // Priority 7: Other specialized libraries
      NATIONAL_MOTTO_ALGORITHMS,
      EXTENDED_CRYSTALS,
      MECHANICAL_SHAPES,
      UNIFIED_SHAPES,
      HYPERCOMPUTATION_SURFACES,
      HISTORICAL_ALGORITHMS,
      GENERATIVE_ALGORITHMS,
      NOISE_FUNCTIONS,
      DIFFERENTIAL_GROWTH,
      ATTRACTOR_SYSTEMS,
      DUAL_MIRROR_ENERGY_SYSTEM,
      VORONOI_SYSTEMS,
      INVERSE_FISHBOWL_SPACE,
      HYDROGEN_ORBITALS,
      CHAKRA_SHAPES,
      REAL_WORLD_OBJECTS,
      FINANCIAL_MATHEMATICS,
      CONSCIOUSNESS_THEORY,
      FOUR_DIMENSIONAL_SHAPES,
      FOUR_DIMENSIONAL_4D_SHAPES, // Include new 4D shapes here as well for lookup

      // Priority 8: Additional math and trig functions
      TRIG_WAVE_FUNCTIONS,
      MATHEMATICAL_SHAPE_IMPLEMENTATIONS,
      ADVANCED_PHYSICS_SIMS,

      // Priority 9: Fallback and recovery shapes
      COMPLETE_MISSING_SHAPES,
      GENERAL_RELATIVITY_FALLBACKS,
      TOPOLOGY_FALLBACKS,
      MISSING_SHAPES_BRIDGE,

      // Priority 10: Base collections (final fallback)
      PARAMETRIC_SURFACES,
      CLEAN_SURFACES,
      EXCLUSIVE_SHAPES,
      NON_EUCLIDEAN_SHAPES,
      RIEMANN_SURFACES,
      EDUCATIONAL_SURFACES
    ];

    for (const collection of allCollections) {
      if (!collection || typeof collection !== 'object') continue;

      const shape = collection[type];
      if (shape) {
        // Normalize the shape to ParametricSurface interface
        if (shape.equation && shape.defaultParams) {
          return shape; // Already correct interface
        } else if (shape.equation && shape.defaults) {
          // Convert from SurfaceEquation to ParametricSurface
          return {
            name: shape.name || type,
            equation: shape.equation,
            defaultParams: shape.defaults
          };
        } else if (shape.x && shape.y && shape.z) {
          // Convert from coordinate functions to equation
          return {
            name: shape.name || type,
            equation: (u, v, params) => {
              try {
                return [
                  shape.x(u, v, params.a, params.b, params.c, params.d, params.e, params.f, params.g, params.h, params.i, params.j, params.k, params.l, params.m, params.n, params.o, params.p, params.q, params.r, params.s, params.t, params.u, params.v, params.w),
                  shape.y(u, v, params.a, params.b, params.c, params.d, params.e, params.f, params.g, params.h, params.i, params.j, params.k, params.l, params.m, params.n, params.o, params.p, params.q, params.r, params.s, params.t, params.u, params.v, params.w),
                  shape.z(u, v, params.a, params.b, params.c, params.d, params.e, params.f, params.g, params.h, params.i, params.j, params.k, params.l, params.m, params.n, params.o, params.p, params.q, params.r, params.s, params.t, params.u, params.v, params.w)
                ];
              } catch (err) {
                console.error(`Error evaluating shape equation for ${type}:`, err);
                return [0, 0, 0];
              }
            },
            defaultParams: shape.defaults || {}
          };
        }
      }
    }

    return null;
  } catch (error) {
    console.error(`Error in getSurfaceEquation for ${type}:`, error);
    return null;
  }
}

// Memory pool for reusing geometry objects
const geometryPool = new Map<string, any[]>();
const maxPoolSize = 10;

function getFromPool(type: string): any | null {
  const pool = geometryPool.get(type);
  return pool && pool.length > 0 ? pool.pop() : null;
}

function returnToPool(type: string, geometry: any): void {
  if (!geometryPool.has(type)) {
    geometryPool.set(type, []);
  }
  const pool = geometryPool.get(type)!;
  if (pool.length < maxPoolSize) {
    // Reset geometry state before pooling
    geometry.dispose();
    pool.push(geometry);
  }
}

export function getDefaultParameters(surfaceType: string): Partial<SurfaceParameters> {
  try {
    const surface = getSurfaceEquation(surfaceType);
    if (surface && surface.defaultParams) {
      return surface.defaultParams;
    }

    console.warn(`No defaults found for shape: ${surfaceType}, using fallback`);

    // Fallback defaults for any shape
    return {
      a: 2, b: 1, c: 1, d: 1, e: 0, f: 1,
      g: 0, h: 1, i: 0, j: 0, k: 0, l: 1, m: 0,
      n: 0, o: 0, p: 0, q: 0, r: 0, s: 0, t: 0,
      time: 0,
      uMin: 0, uMax: 1, vMin: 0, vMax: 1,
      uSegments: 32, vSegments: 32
    };
  } catch (error) {
    console.error('Error getting default parameters for', surfaceType, ':', error);

    // Safe fallback
    return {
      a: 2, b: 1, c: 1, d: 1, e: 0, f: 1,
      g: 0, h: 1, i: 0, j: 0, k: 0, l: 1, m: 0,
      n: 0, o: 0, p: 0, q: 0, r: 0, s: 0, t: 0,
      time: 0,
      uMin: 0, uMax: 1, vMin: 0, vMax: 1,
      uSegments: 32, vSegments: 32
    };
  }
}