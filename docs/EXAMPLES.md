# API Examples

## JavaScript Concept

```javascript
const response = await clouud.message({
  input: "Analyze this structure",
  provenance: true
})

console.log(response)
{
 "message": "...",
 "verification": "...",
 "assessment": {}
}
```bash
git status
