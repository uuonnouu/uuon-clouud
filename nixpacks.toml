[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm install --include=dev --legacy-peer-deps']

[phases.build]
cmds = ['npm run build:client', 'npm run build:server']

[start]
cmd = 'node dist/index.js'
