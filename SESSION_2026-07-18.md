# Session Record — 2026-07-18
Repo: uuon-clouud (private) · All changes pushed to main

## COMPLETED
1. Linguistic layer v1 shipped (linguistics/)
   - phonetic_layer.py — pinned metric ipa_segment_levenshtein_v1;
     diphthongs/affricates/long vowels = single segments
   - homophone_inventory.py — priors computed by the metric at load,
     zero hand-entered numbers
   - test_phonetic_layer.py — gate test; reproduces canonical
     0.889 phonemic / 1.0 phonetic. PASSED.
2. UUON_C_Bot integration POSTPONED, blockers documented in
   PROJECT_STATUS.md: no payment execution (SQLite-only accounting),
   invented metrics in /start (99% reduction, €2.34M savings),
   spoofable auth header, personal wallet hardcoded, ephemeral DB.
3. GitHub PAT exposure — token was embedded in remote URL; rotated,
   remote URL cleaned to tokenless form. Incident logged.
4. "gitsafe" unknown remote — investigated, no data ever transferred,
   concluded Replit-internal backup service; removed. False alarm
   logged as a worked example of flag → evidence → downgrade.
5. Workspace reconciled with July-17 remote session (rebase; kept
   remote railway.json + clouud-ai.ts). Typo artifacts removed,
   backups/ gitignored.
6. Credential purge from FULL git history (filter-repo, force-push,
   verified by tree grep = HISTORY CLEAN):
   Pi Wallet Key.md, Etsy Backup Codes.md, api-keys.md (Tripo3D),
   nToken Replit Passphrases.md, BTC/ETH fragments, d13mon4 corpus
   copy, PHILTER zips — BOTH locations (clouud-observer/ and root
   corpus/). Local extracts saved to ~/extracted-secrets/ pre-purge.

## PENDING — yours, nothing blocks on me
- [ ] Rotate Tripo3D API key (platform.tripo3d.ai → API keys)
- [ ] Regenerate Replit passphrases/tokens (Account → Security)
- [ ] Regenerate Etsy 2FA backup codes (Account → Security)
- [ ] Download ~/extracted-secrets/ (2 files) off Replit, then delete
- [ ] Confirm old ghp_ token DELETED on github.com token list
      (rotated ≠ revoked; verify it is absent)

## STANDING (pre-existing, unchanged)
- D13MON4 original in Dmension repo — extract to private repo
- Next build task: replace 6 hand-picked homophone pairs with
  CMUdict-derived inventory (priors from the lexicon, not memory)

## Lesson logged
A purge is done when the tree grep is empty, not when the command
exits. First filter-repo run missed the root corpus/ duplicate;
caught by verification step, fixed with basename globs.
